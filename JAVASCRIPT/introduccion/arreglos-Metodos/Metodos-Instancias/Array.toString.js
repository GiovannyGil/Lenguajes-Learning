/**
 * Array.toString(
 * 
 * es un metodo que transforma un array en una cadena string separando los elementos por una ","
 */

const array1 = [1, 2, 'a', '1a'];

console.log(array1.toString());
// Expected output: "1,2,a,1a"
