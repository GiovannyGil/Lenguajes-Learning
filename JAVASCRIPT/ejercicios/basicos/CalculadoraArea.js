/**
 * Calculadora de área:
 * Escribe una función que calcule el área de un rectángulo. La función debe recibir el ancho y el alto como parámetros y devolver el área.
 */

function calcularArea(ancho, alto){
    let area = ancho * alto
    return area
}


areaRec = calcularArea(3, 15)
console.log(areaRec);
