/**
 * Array.shift
 * 
 * es un metodo que elimina el primer elemento de un array, esto cambia los elementos y el tamaño de un array
 */

const array1 = [1, 2, 3];

const firstElement = array1.shift();

console.log(firstElement);
// Expected output: 1

console.log(array1);
// Expected output: Array [2, 3]
