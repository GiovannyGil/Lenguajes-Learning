/**
 * Array.length()
 * 
 * 
 * es un array que devuelve la cantida/tamaño (count) de un array
 */

const clothing = ['shoes', 'shirts', 'socks', 'sweaters'];

console.log(clothing.length);
// Expected output: 4
